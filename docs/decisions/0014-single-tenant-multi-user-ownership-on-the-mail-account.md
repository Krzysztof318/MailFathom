---
status: proposed
contact: Krzysztof Kasprowicz
date: 2026-08-25
deciders: Krzysztof Kasprowicz
consulted:
informed:
---

# Serve several users from one deployment, hang ownership on the mail account rather than on the mail, and answer multi-tenancy with a second instance

<!-- describes: backend/src/Application/Access/**, backend/src/Application/Accounts/**, backend/src/Application/Emails/Mailboxes/**, backend/src/Application/Emails/GetEmailContent/**, backend/src/Application/Emails/DownloadAttachment/**, backend/src/Application/Mail/Delivery/Authoring/**, backend/src/Application/Mail/Delivery/Submission/**, backend/src/Application/Mail/Mutations/Authoring/**, backend/src/Infrastructure/Persistence/Emails/StoredEmailSelectionPredicate.cs, backend/src/Infrastructure/Persistence/Emails/StoredEmailTimelineReader.cs, backend/src/Infrastructure/Persistence/Emails/Threads/**, backend/src/Infrastructure/Persistence/Entities/**, backend/src/Infrastructure/Persistence/Accounts/**, backend/src/Infrastructure/Persistence/Users/**, backend/src/Infrastructure/Persistence/Contacts/**, backend/src/Infrastructure/Persistence/MailFathomDbContext.cs, backend/src/Application/Rules/Evaluation/MailRuleScheduleSource.cs, backend/src/Application/Jobs/Scheduling/JobSchedulePass.cs, backend/src/Host/Api/AdminAccountRequest.cs, backend/src/Host/Configuration/Mail/MailSynchronizationOptions.cs, backend/src/Host/Configuration/Mail/MailAccountNamingSpace.cs, backend/src/Host/Configuration/UserSettings/** -->

## Context and Problem Statement

MailFathom serves one user's mail. Accounts are declared in read-only configuration, no persisted entity carries a discriminator for whose data a row is, and ADR 0012 names *the deployment serves one user's mail* as a decision driver — which is why issue 880 cut the account-restriction axis out of that record and left issue 588 waiting on a decision nobody had made. Issue 889 is that decision, and this record is its answer. Issue 588 has since been closed as not planned, because the section below refuses the axis it was left holding rather than leaving it standing there.

Two target shapes were on the table and they are layers rather than alternatives: one tenant with several users, each owning a set of mail accounts; and several tenants, with the option of a PostgreSQL database per tenant. The second does not remove the need for the first — if a tenant holds several users, the axis of *whose data is this* has to exist in the rows regardless, and a database per tenant only adds routing above it. So the question is not only which shape to build but whether the second is worth building at all, and the answer decides how much of the first has to anticipate it.

The decision was taken after reading the persistence layer, the read paths, the background workers, and the indexes rather than from the shape of the problem. Four findings from that reading changed what the answer had to say, and each is stated where it bears below:

**`mailbox_accounts` holds nothing but what identifies an account.** It carries an identifier and the user it belongs to, keyed by the two together and cascading from `user_accounts`, created lazily by whichever synchronization run first binds one of the account's folders. `mail_folders` cascades from it, `stored_emails` cascades from the folder, and every table derived from a message cascades from the message — so a user hung on that row reaches all of the mail without a column on the table that will hold it. Sixteen entities carried `MailboxAccountId` when this was read, but only three key onto the account, which is what separates what the user *reaches* from what a deletion *discharges*; the erasure section below is where that gap is stated rather than smoothed over. What stays true of this finding is its reach argument — the cascade carries whoever is reached through the account row to all of the mail — and it is the part the sections below build on. What it does not settle is what the account is keyed by and how a user reaches it, which is answered under *A mail account is a record of its own, and users are assigned to it*.

**Nothing in the mail read path uses raw SQL.** `IgnoreQueryFilters` appears nowhere in the repository, and every use of `ExecuteSql`, `ExecuteSqlRaw`, `SqlQuery`, and `SqlQueryRaw` lives in one of eleven files, none of which returns mail: the job store and its schedule and dead-letter companions, the outgoing-mail claim, embedding generation, the spend ledger, the refresh-token store, the repair-request store, the content table's own size, schema inspection, and the vector index's DDL. The premise that made PostgreSQL row-level security the only mechanism below the ORM is therefore absent here.

**The approximate vector index already exists, and the ranking query cannot use it.** `EmbeddingProfileVectorIndex` builds a partial HNSW index per embedding profile at activation, and `EmailVectorSearchIndexReader` ranks emails by a correlated minimum over each message's chunks with the structural filters joined — a shape that index cannot serve, as that class says in its own remarks. So *what a filtered vector search does once an ANN index exists* is not a future question; it has an answer today, and ownership neither causes it nor changes it.

**Two ceilings, a queue, and a cursor are deployment-wide with no user in them.** The embedding spend ledger is keyed by period alone; the stored-content ceiling is `pg_total_relation_size` over one table; the job claim orders by `AvailableAt` and the identifier, which is pure FIFO across every user; and each named backfill keeps one cursor for the whole deployment.

## Decision Drivers

- **A deployment is already cheap to run twice.** One container, one database, one configuration file, and a Helm chart or a Quadlet unit that installs it. Whatever multi-tenancy would buy has to be worth more than the instance it replaces, and for the shape this project has it is not.
- **The user axis is irreversible and the routing above it is not.** Where ownership lands in the data model is a migration over data somebody is running; a database per tenant added later rewrites no entity. So the order is fixed whatever is decided about the second layer, and the second layer's absence must not be allowed to shape the first badly.
- **Ownership is not a permission, and ADR 0012 must not be reopened to say so.** A permission names *what may be done* and scoping names *on what*. Adding `mailfathom.mail.read.account.<id>` would reopen exactly what that record closed, and the published set would stop being closed.
- **Isolation has to be a property of the system rather than of the care taken in each repository method.** An operator running one deployment for several people needs one person's mail unreachable from another's credential even where a query is written wrong.
- **One mailbox is one mailbox.** A team that reads `office@` together reads one set of messages, one set of flags, and one set of rules; a deployment holding a copy per reader synchronizes the same server several times, disagrees with itself about what is read, and multiplies what an erasure has to reach.
- **One schema, one migration history, one snapshot gate, and one upgrade artifact is a contract rather than a habit.** ADR 0009 states all four, the `Pending model changes` job enforces the third, and the schema artifact beside the `mfctl` binaries is how an operator brings a database to a release. Multiplying databases multiplies every one of them.
- **A first deployment has to work before it has a user model.** This is somebody's own mail server as often as it is an organization's, and the person standing one up configures a mailbox and a credential. A design whose first step is a user-provisioning decision is one most of its users meet as an obstacle.
- **Erasure has to be dischargeable and provable.** Dropping a database discharges a deletion request completely; a shared database has to reach mail, embeddings, and the content store, and then reach every table that records an account without a foreign key onto one, and then show that nothing was left.
- **A break here is affordable and still has to be written down.** ADR 0004 permits a `0.y.z` minor to break the configuration schema, the MCP tool contract, the database schema, and the deployment contract, so what a change decides is which shape is right, never whether the break may be taken.

## Considered Options

- **One tenant, several users, ownership on the mail account.**
- **Several tenants, a PostgreSQL database per tenant, with a role per tenant.**
- **Several tenants inside one database, separated by a tenant discriminator and row-level security.**
- **Stay single-user and refuse the whole question.**

## Decision Outcome

Chosen option: **one tenant, several users, ownership on the mail account**, because it is the only one of the four that hangs the irreversible half of the change on the axis the data model already reaches every mail table by, and because the two multi-tenant options both spend their cost on operations to buy a separation a second instance of this deployment already gives.

**Multi-tenancy is refused rather than deferred.** An operator who needs two tenants runs two instances. That is not a smaller version of the tenanted shape: it is stronger separation than a database per tenant with a role per tenant, because there is no routing to get wrong and no process holding both, and it costs a container and a database — which this project already asks an operator to run once.

The rest of this section settles each question issue 889 lists, and the ones sharing a mailbox between users raised after it. Every implementation issue this record authorizes is written against these answers, and one that departs from an answer says so on its own issue rather than deciding it again.

### A principal is a user, a user is assigned accounts, and the bound belongs to the user

A **user** is who mail is served to. A **mail account** is the mailbox, and it is assigned to one or more users; the mail beneath it is readable by every user it is assigned to and by nobody else.

A **credential** belongs to a user and reaches the accounts assigned to that user. The bound is therefore a property of the user rather than of the credential, which settles the question issue 588 was left holding: two mailboxes an operator wants separated are two users, not one user with two narrowed credentials.

`AuthorizedPrincipal` gains the user it is acting for and carries nothing else new. Its permissions stay ADR 0012's closed set, unchanged in vocabulary and in meaning. The process identity acquires no user, because work no caller requested is not acting for one; a use case reachable by a caller refuses it exactly as it does today.

**Narrowing one credential within its user's own accounts is refused rather than left open.** It is what issue 588 asked for once the two-mailbox case is answered by two users — a user confining an agent to a corner of their *own* mail — and that issue is closed as not planned. A credential reaches every account assigned to the user it resolved to, and nothing narrows it further. Neither reason is that the bound would be hard to build. The first is that a second bound is a second scoping rule standing beside the user narrowing, which every narrowing site this record names would then have to keep consistent with the first forever; that is how isolation stops being a property of the system and becomes a property of the care taken at each site, which is the decision driver above read the other way round. The second is that the unit mail is served to is the user, so two mailboxes an operator wants separated are two users, which this record already provides and which nothing else has to say a second time.

**The scope is transparent to the caller, and that is what makes one rule enough.** A surface a user reaches resolves exactly one user from the credential that authenticated, and every MCP tool call and every request to the client API is answered within the accounts assigned to that user. The scope is therefore never something such a caller states: no tool argument, no request field, and no header names it, nothing they send widens it, and an agent holding a credential is correctly bounded without knowing that any of this happened. What a request may still do is name accounts, and that stays a filter over what the caller already reaches — the caller choosing what to look at — rather than becoming a statement of what it may look at. An account not assigned to the caller's user is refused exactly as an account this deployment does not serve, which is what keeps the filter from being read as a bound in either direction.

**Two things stand outside every sentence of that paragraph**, and they are named here rather than left for a reader to notice. The first is the administrative operations below: a deployment administrator acts for no user, so nothing resolves one from their credential; the operations they reach reference the deployment-wide catalog rather than the caller-scoped one, and name an account by its generated identifier, which is unambiguous across the deployment. *Ownership is not part of the permission vocabulary, and here is where the check runs* decides that per operation rather than per surface, and *An account is named by a generated identifier and is unique by its address* is where the naming is settled. The second is the attachment download, which answers on a route carrying no authenticated caller at all: ownership reaches it through the signed capability the URL carries rather than through a credential, which is a property of the ticket rather than of the read, and the same section states it under *Three of the four identifier reads run for a caller*.

### A mail account is a record of its own, and users are assigned to it

**A mail account is a record of its own**, not a part of any user's record. It carries an identifier the deployment generates, the email address of the mailbox, and every setting of the account. **The relation between users and accounts is many-to-many**: an assignment names one user and one account, and cascades from both.

**Only the administrative surface assigns an account to a second user.** A user adding an account through the client assigns it to themselves alone, and nothing a user reaches assigns one of their accounts to somebody else. Sharing a mailbox is therefore an operator's act, taken where the operator can see every user it reaches, rather than something one user extends to another.

**The mail an account holds is one copy, keyed by the account identifier.** `mailbox_accounts` is keyed by the generated identifier alone, every table that names an account names it by that identifier, and where a foreign key onto the account exists it points at that one column. No table holding what the mailbox holds carries a user: whose mail a row is follows from the assignments of the account it belongs to, so assigning a user reads nothing into the rows and unassigning one rewrites none of them.

**Authored mail is the one place a user stays on the row, as its author rather than as its owner.** A draft is what one person is writing and has not sent, so it belongs to its author: `mail_drafts` keeps the user beside the account, a draft is read only by its author among the account's assigned users, and erasing that user takes their drafts with them. An outgoing email is a message the mailbox sends, so once submitted it belongs to the account like the sent mail it becomes: every assigned user reads it, erasing its author does not withdraw it from the mailbox, and the author it records is a reference the erasure seam reaches on the same terms as an audit trail's actor below. A recurring send is a standing instruction one person gave and has not withdrawn, so it belongs to its author like a draft: `recurring_sends` keeps the user beside the account and its uniqueness identity keeps the user in it, so two users assigned one mailbox declaring the same requester identity hold two declarations rather than one; it is read and stopped only by its author among the account's assigned users, or by an administrator; and erasing its author stops and removes it, so a mailbox still assigned to somebody else never goes on sending an erased person's message. What each occurrence produces is an outgoing email, and belongs to the account as one. An outgoing email keeps its author in its idempotency identity for the same reason a recurring send does — two assigned users retrying the same requester's submission are two submissions — while every assigned user reads it. None of these user columns is a narrowing term for anything but the draft and recurring-send reads, so the rule that every other narrowing site reads the caller's assigned accounts is untouched by them.

**Every narrowing site reads the accounts the caller is assigned.** The caller-scoped catalog answers with the accounts assigned to the request's user, the resolution turns that into the scope, and the predicate a scoped read composes is `MailboxAccountId = ANY(...)` over it. Every site named under *Ownership is not part of the permission vocabulary* narrows that way, and none of them narrows by a user column, because no mail table has one.

**This section and the one on the account identifier below amend what this record previously decided**, on 2026-09-13 under issue 1949 and in this record rather than a superseding one, because the record is still `proposed` and an ADR at that status is editable. The shape they replace keyed an account by its user and an operator-chosen identifier together and carried the user beside every reference to an account. That shape answered an identifier being ambiguous between two users by scoping the identifier to its user; it could not answer one mailbox being read by two users without holding two copies of it, each with its own settings. A generated identifier removes the ambiguity at the source instead, so the user no longer has to travel with the key to disambiguate it. The same amendment reaches *Every setting of an account lives on the account*, *Organizations group users and scope a login*, the per-user ceilings under *The deployment-wide mechanisms*, *Contacts belong to the user who wrote them or the account they arrived on*, *Erasure*, the destructive operations under *The administrative surface gains a deployment administrator*, the delivery order, and the consequences and validation that follow them. The refusal of multi-tenancy, the permission vocabulary, the places the scoping check runs, the refusal of row-level security, and the move out of configuration stand as written, read with *assigned to* where they speak of accounts a user holds.

**An identity composed as text from the identifier needs nothing added once the identifier is unique.** `MailRuleScheduleSource.Declare` builds a schedule identity as `mail-rules:{accountId}:{ruleName}`, that string is `job_schedules`' primary key, and `JobSchedulePass.ComposeKey` derives the job idempotency key from it — so all three rest on the identifier being unique across the deployment. An operator-chosen identifier is not: two users each declaring `work` would compose one schedule row and one idempotency key per occasion, and one user's scheduled walk would be answered as already enqueued and never run. A generated identifier is, so **every such composed identity names the generated identifier**, and a composed identity naming a display name or an address instead is the failure this paragraph describes.

**What the key enforces is narrower than the ambiguity it removes, and the difference matters to whoever implements this.** On the four tables that carry a foreign key onto the account — the folders, the threads, the jobs, and the collected contacts — a reference is a reference to exactly one account. On every other table the account is a plain column, and a read that narrows on nothing at all still compiles. So this record does **not** claim that the key makes a forgotten narrowing impossible: isolation of a read stays on `StoredEmailSelectionPredicate.WithinScope`, which every mail-returning read composes — the thread read included, which keeps no second copy of the narrowing — and on the tests under *Validation*. What the key buys is that the value those predicates compare names one mailbox, which is what makes a correct narrowing expressible at all.

The cascade is unchanged and still does its work: `mail_folders` carries the account and cascades from it, `stored_emails` carries it too and cascades from the folder, and every derived table — contents, search documents, chunks, embeddings, classifications, threads, mutations, executions, audits, outgoing mail — either carries the account or reaches it through a foreign key.

**A user assigned no account resolves to a scope that reads nothing, and never to an empty account list.** That distinction is the whole of the mechanism's safety, because an empty list is not *nothing* in the code this decision reuses: every narrowing site applies the account predicate only when the list is non-empty — `StoredEmailSelectionPredicate.Matching` and `StoredEmailThreadReader.Readable` alike — so an empty one is read as *unrestricted*. Today two things keep that convention closed: `StoredEmailSelectionPredicate.WithinScope` compares the row's user ahead of the account list and unconditionally, and `MailboxScope.NothingReadable` names the unspecified user, which matches no row, and admits no folder. **This amendment removes the first of those**, because a mail row stops carrying a user to compare — so a user provisioned before an account is assigned to them, or left after their last one is unassigned, is kept from every other user's mail only by the resolver answering with `NothingReadable` and by that scope admitting no folder. Resolving such a caller to an empty account list instead would open every other user's mail through the narrowing site's own convention, so an implementation that drops the user term and changes nothing else about the resolver's answer would fail open rather than closed.

**The timeline across several accounts stays a merged per-account walk.** An index leading with `MailboxAccountId` serves one account as an ordered walk, but across several accounts PostgreSQL derives an ordered path from partitioning or from appended paths and never from `= ANY`, so *all of my mail* read through one index would be planned as a scan of everything that matched followed by a top-N sort. `StoredEmailTimelineReader.Page` answers that by branching to `MergedPage` for a scope naming more than one account — a keyset per account merged on the same ordering — and keeping the single-account query for a scope naming one. With no user on a mail row there is no leading column that would serve the multi-account read in one walk, so `MergedPage` is the answer rather than a stopgap for an index, and the index the timeline takes leads with the account. Its shape has to agree with `EmailTimelinePosition.NewestFirst` column for column, or the continuation cursor stops being contiguous.

Two things follow for every other read. The lexical ranking already computes `ts_rank` over everything that matched before taking a window, and the vector ranking is already exact, so neither acquires a new bound from ownership and neither is relieved of the one it has. And the measurement that settles any index choice here is a corpus large enough for the account predicate to stop being selective on its own — the existing index test seeds six hundred rows, which is enough to prove an index is chosen and not enough to prove anything about a mailbox somebody actually has.

**What this costs is a rekey of the mail graph** onto the generated identifier — every table naming an account moves from the pair it names today to the one column — carried as an additive migration over existing rows, which is derivable because every existing account has exactly one user to assign and one identifier to mint.

### Every setting of an account lives on the account

**Everything that configures a mailbox belongs to the account record and applies to every user it is assigned to**: the server and its credentials, including the sealed refresh token; synchronization and its window; folders and their mapping; spam classification; sensitive content; contact collection; and the audit trails of what was done to the mailbox — mutations, rule runs, answering runs, and classification runs. A user's record keeps what is the person's own: their display name, their language, their credentials, their surface switches, and their own contacts. **The secrets an account's server credentials are sealed as belong to the account too**: `stored_secrets` rows an account declares are keyed onto the account rather than onto the user who declared them, so erasing that user never takes a credential a mailbox still assigned to somebody else needs to connect.

**A rule stays where ADR 0010 declares it and acts on the account.** A rule set is a section of the deployment's configuration, and a rule names the accounts it reaches; this record moves no rule into the account record and adds no rule table. What it settles is what a rule naming an account reaches: the account, and therefore every user assigned to it. A rule names an account by its address or by its generated identifier, both unique across the deployment, and never by a display name, which is unique only within one user's assigned accounts and would leave a deployment-wide rule naming whichever `work` resolved first.

**The trade-off is stated rather than left implicit: a setting one assigned user changes reaches every other.** A rule naming the account files mail for all of them, a folder one of them withholds is withheld from all of them, and a spam threshold one of them lowers moves messages every one of them reads. That is the correct answer for a mailbox rather than a regrettable one — the mail is one copy, so a rule filing it moves it for everybody whatever the settings say, and per-user settings over one copy would disagree with the server about where a message is. An operator who wants two people to configure a mailbox differently is describing two mailboxes.

**An audit trail belongs to the account for the same reason.** It records what happened to the mailbox, and every user assigned to it reads the mailbox that resulted, so each of them reads the whole trail rather than the part they caused.

**A notification is the one thing about a shared mailbox that stays per person.** What somebody has been told and has dismissed is theirs, so mail arriving on a shared account writes one notification per assigned user, each deduplicated within that user as `(UserId, DeduplicationKey)` already is, and one user dismissing theirs dismisses nobody else's.

### Ownership is not part of the permission vocabulary, and here is where the check runs

No permission names an account. ADR 0012's set is untouched and stays closed. The scoping check runs in five places and each is named, because between them they are every path that resolves an account for a caller. That is deliberately wider than *every path that returns mail*, which was the first enumeration this record attempted and which is what let the send and the account listing fall outside it — neither returns mail and both resolve an account:

**The resolution, for every scoped read.** The account catalog is split in two. `ICallerMailAccountCatalog` answers with the request's user's accounts — what this amendment makes the accounts assigned to that user — and is what `MailboxScopeResolver` reads; `IDeploymentMailAccountCatalog` answers with every served account and is what the synchronization coordinator and the workers read. **The two are told apart by name rather than by reach, and that is a limit on what the split can enforce.** Both have to be resolvable from `Application`, because use cases there need each: `MailboxScopeResolver` needs the caller-scoped one, while `MailSynchronizationStatusReader` and `MailRuleScheduleSource` answer for the deployment and need the process-wide one. Both are registered in the one container every use case resolves from, so nothing about the composition stops a read model injecting the wrong catalog — it compiles and it answers across users. The split makes the wrong one *nameable* rather than unreachable, and what holds a caller-facing read to the right one is a test rather than the compiler.

**The administrative surface reads whichever the operation is, and that is decided per operation rather than per surface.** It is the one place a user and a deployment administrator share routes: `MailboxMaintenanceEndpoints`, `MailFolderErasureEndpoint`, `MailAnsweringAuditEndpoint`, `MailboxMutationAuditEndpoint`, `MailRuleEndpoints`, `SpamClassificationEndpoints`, and `JobDeadLetterEndpoints` resolve an account through `AdminAccountRequest` rather than through `MailboxScopeResolver`, and the answering audit returns mail a run cited. An endpoint cannot read both catalogs, so the rule is on the operation: one reached with an administrative permission reads the process-wide catalog, and one a user reaches about their own accounts reads the caller-scoped one. An operation that is both is two operations.

**The identifier reads, for the four paths that reach an email without building a scope.** `EmailContentReader`, `EmailAttachmentDownloadReader`, `StoredEmailResponseAuthoring`, and `MailFlagChangeRecorder` each look an email up by its stored identifier and then ask `MailboxScopeResolver.IsReadableByTools` whether the account and folder it turned out to be in are readable. That question is answered from the account's settings and becomes a question about the caller's assignments as well. It is one method, asked in four places, and it stays one method for the reason it already is one: a fifth path added later asks the same question or does not read mail.

**The account listing, which publishes accounts rather than mail.** `MailAccountDirectoryReader.ReadAsync` is what `list_accounts` reaches. It requires `MailFathomPermission.MailRead` and then takes the caller-scoped catalog's accounts directly for the set it publishes; `MailboxScopeResolver` appears in it only to resolve the folder freshness beside each account, so the account list itself never passes through the resolution. Its own remarks already say what that answer is — *naming the accounts a deployment serves is publishing that they exist* — so were it to read the deployment-wide catalog, the accounts it names would be everybody's, published to any caller holding `MailRead`. It reads the caller-scoped catalog, and a user assigned no account gets an empty directory rather than the deployment's.

**The send, which returns no mail and is therefore the path an enumeration of reads misses.** `AuthoredMailSubmission.SubmitAsync` takes the account the caller named and resolves it off the caller-scoped catalog, reaching neither `MailboxScopeResolver` nor `IsReadableByTools`; the only thing it checks first is that the caller holds `MailFathomPermission.MailSend`. Resolved off the deployment-wide catalog instead, a caller holding `MailSend` could name an account not assigned to them and mail would leave as that mailbox, which is a worse outcome than any read this section bounds. So the submission resolves against the caller-scoped catalog like every other caller-facing resolution, and an account not assigned to the caller's user is refused exactly as one the deployment does not serve. Every user assigned a shared account may send from it, since sending as the mailbox is part of reading it. The reply and forward paths need nothing added — `StoredEmailResponseAuthoring` derives the account from an email it has already put through `IsReadableByTools` — so it is the new-message path alone that this names.

**Three of the four identifier reads run for a caller. The attachment download does not, and it is the only path that streams an attachment's octets.** `EmailAttachmentDownloadReader` calls `RequireSignedCapability`, and `EmailAttachmentDownloadEndpoint` assumes `AuthorizedPrincipal.SignedCapability` on a route with no authenticated caller — so there is no caller's user for it to ask about, and the amended `IsReadableByTools` cannot be what bounds it. **Ownership reaches it through the ticket instead:** a signed capability records the user it was minted for, and redemption checks that the email the ticket names belongs to an account still assigned to that user, so a ticket minted before ownership existed does not redeem, one minted for one user does not serve another who holds the URL, and one minted before its user was unassigned stops redeeming when the assignment goes. That is a property of the capability rather than of the read, which is why it is stated here rather than folded into the sentence above.

A request naming an account not assigned to the caller's user is answered exactly as one naming an account this deployment does not serve — the same failure, with nothing in it that separates *not yours* from *not here*, so a refusal cannot enumerate what exists.

### Row-level security is refused, with the condition that would reopen it

Issue 889 reasoned that EF Core global query filters cover the common route and miss raw SQL, `SqlQuery`, `IgnoreQueryFilters`, and lookups by identifier, leaving PostgreSQL row-level security as the only mechanism below the ORM. **Two of those three premises do not hold here, and the third is answered above.**

`IgnoreQueryFilters` appears nowhere in the repository. Raw SQL is confined to the eleven files named above and not one of them reads mail. The identifier lookups are the four paths named above too, and they are gated by a method rather than by a filter, so a query filter was never what protected them.

Refusing row-level security also avoids two costs that are real rather than theoretical. Under Npgsql a session setting made without `SET LOCAL` survives into the next request on the same physical connection, so a per-request identity carried that way is a defect waiting for a pooled connection to be reused — the mechanism would have to be designed around a trap rather than merely adopted. And the posture it requires costs something, though less than it first appears. Table ownership is **not** the obstacle: PostgreSQL exempts an owning role from its own policies by default, but `ALTER TABLE ... FORCE ROW LEVEL SECURITY` binds them to the user too, which is exactly the arrangement here — the serving role owns `email_embeddings` so `EmbeddingProfileVectorIndex` can create and drop the partial index at profile activation, and a forced policy would still hold against it. What remains is `BYPASSRLS`, which no policy survives and which is therefore a standing property of the role rather than something a statement can force, and the `SET LOCAL` trap above. So the cost is real but narrower than a choice between the index's lifecycle and the policy — that dilemma does not exist, and this record states it so that whoever reads the reopening condition below is not told the database refuses something it permits.

What stands in its place is that the predicate is written once for the reads that share it. `StoredEmailSelectionPredicate.Matching` is where a mailbox read is narrowed, all three selection-driven read models compose it — the timeline, the lexical ranking, and the vector ranking — and the tombstone exclusion already leads it precisely so no caller can opt out. The narrowing to the caller's assigned accounts joins it there.

**A fourth mail-returning path composes the same narrowing rather than a copy of it.** `StoredEmailThreadReader` returns subjects, senders, and sent-at instants and is read by membership rather than by a selection, so it does not compose `Matching`; its `Counted` and `Readable` compose `StoredEmailSelectionPredicate.WithinScope` instead, which leads with the tombstone exclusion and carries the account containment and the admitted and withheld folders, and the thread read states none of its own. So the property that replaces row-level security is *every mail-returning path composes one narrowing*, and the thread read is held to it by the same test as the three selection-driven readers — a validation reaching only those three would pass while a fourth path wrote a second copy nothing checks.

**The condition that reopens this: a mail-reading path that takes raw SQL.** If one is ever written — a ranking the query provider cannot express, a statement composed for a plan — then the premise this refusal rests on is gone and row-level security becomes required rather than optional. That is a rule an implementation can be checked against rather than a hope.

### Accounts, users, and credentials leave configuration together

Users arrive and depart while the process runs, so the three move into the database as one change: the user, the mail account, and the credential that admits a caller acting for that user.

Moving fewer than three is worse than moving none. Accounts in the database with credentials still in a file means a restart for every user added; users in the database with accounts still in a file means an account nobody is assigned.

What stays configuration is what belongs to the deployment rather than to a person or a mailbox: the synchronization cadence and its bounds, the shape and default of every ceiling, the embedding profile declaration, rules authored under ADR 0010, endpoints and their transport authentication, and the data-encryption key ring under ADR 0005.

**This costs ADR 0002 nothing**, which was checked rather than assumed. That record closes the write side — configuration is read-only to the process, and administrative editing of it, an approval workflow, a configuration history, and *a multi-tenant configuration lifecycle* are refused rather than deferred — and it then says that every question about state a program modifies belongs to the record introducing that state and is answered in PostgreSQL. Accounts, users, and credentials become exactly that: state the program modifies, answered in PostgreSQL, introduced by this record. The refusal of a multi-tenant configuration lifecycle is agreed with rather than strained, because this record refuses multi-tenancy outright.

### An account is named by a generated identifier and is unique by its address

**A mail account's identifier is generated by the deployment**, a version 4 UUID rather than a string an operator types. It is what a row, an index, a job, a lease, and a composed identity name, and it is what an administrative operation names — so a deployment administrator naming an account never meets two accounts answering to one name, and `AdminAccountRequest` resolves exactly one or none. It is version 4 rather than the time-ordered values the rest of persistence mints for the reason the user identifier is: it reaches the MCP tool contract and administrative listings, and a time-ordered value published there would say when each mailbox was added and in what order.

**The deployment-unique key of an account is its email address, compared on its normalized form.** The address is required before an account is served, a unique index over the normalized form of every address that is set holds it across the deployment, and two writers claiming one address at once are separated by that index rather than by a read before the write. This is what makes *one mailbox is one mailbox* a property of the schema: a second user who needs to read `office@` is assigned the account that already holds it rather than declaring a second copy. **A user adding, through the client, an address that exists anywhere in the deployment is refused without being told it exists** — the refusal reads the same whether the address is held by another user, by nobody, or is merely invalid in a way the index is what catches — because an answer that distinguished them would let any user probe which mailboxes the deployment serves.

**An account is published to a user under a display name, unique within the accounts assigned to that user.** A request may name an account by its generated identifier or by that display name, and both resolve only among the caller's assigned accounts. **The display name is a column of the account**, like every other setting of it, so one mailbox answers to one name for everybody it is assigned to. The rule `MailAccountNamingSpace` holds over configured declarations today moves onto the write path of the account record and its assignments, where it judges the whole assigned set of every user a write reaches, and both writes that can make a set hold two accounts under one name are held to it: assigning an account to a user who already holds another under the same display name is refused naming the collision, and renaming an account is refused when the new name collides in the assigned set of any user it is assigned to. A user renaming a shared account is told only that the name is taken for somebody the account is assigned to, never which account holds it, because the other user's accounts are not theirs to enumerate; an administrator is told both; two users each holding a `work` that are two different mailboxes is no collision, since neither user can name the other's. The exemption the configured rule carries — a name equal to its own account's identifier — has nothing left to exempt once the identifier is generated, and goes with it.

Per ADR 0004 the movement is named against every surface it reaches, and there are three rather than two.

**MCP tool contract** — breaking. An account identifier becomes a generated UUID, and `list_accounts` publishes it beside the display name; both resolve only within the caller's assigned accounts, and a client naming an account not assigned to its user is answered exactly as one naming an account the deployment does not serve, since a refusal may not separate *not yours* from *not here*. The migration writes each account's former identifier as its display name where the account carried none, so a client naming an account by the identifier it typed keeps resolving; the operator's action is to re-point any client naming an account whose display name was set to something else.

**Configuration schema** — breaking. The account is no longer configured, and it is no longer part of a user's record either: its keys leave the user record and the operator-typed identifier is withdrawn, so validation that used to accept them fails. A rule under `MailRules` names the accounts it reaches by address or generated identifier, so a rule naming an operator-typed identifier is refused when the section binds. The operator's action is to provision what they had configured, and to rewrite each rule's accounts as the addresses of the mailboxes it was meant to reach; nothing imports a configured account for them.

**Database schema** — breaking, and it takes ADR 0004's **major** row rather than being classified by the implementation. `mailbox_accounts` becomes a record keyed by the generated identifier and carrying the address: the migration drops its `(UserId, Id)` key and its cascade onto the existing `user_accounts`, and an assignment table appears between the two in their place. Every table referencing an account is rekeyed onto the identifier with the index rebuild that follows, and `user_stored_content` is rekeyed onto the account and renamed `account_stored_content`, with a user's figure summed over their assignments, while `stored_content_claims`, which reserves headroom before a store, is rekeyed onto the account with it. The account record carries its display name and the document of its settings, which leave the user's `settings_accounts` document; `mailbox_refresh_tokens` and the `stored_secrets` rows an account declares are rekeyed onto the account; and `contacts` gains a key onto the account for collected contacts beside its key onto the user. `contact_addresses` moves with it: its key onto `contacts(Id, UserId)` becomes a key onto the owning contact alone, and its `(UserId, NormalizedAddress)` uniqueness becomes uniqueness within the book that contact belongs to — the user's for their own contacts, the account's for collected ones — which is what lets a collected contact hold addresses at all and lets one address be held both as a user's own contact and as a collected one, the case the merged read resolves in the user's favour. An organization table appears, the user row gains a nullable organization, and `user_credentials` gains the organization a password credential is scoped to, with its lookup index widened to `(Method, OrganizationId, Lookup)` under `NULLS NOT DISTINCT`; an existing credential is in no organization and signs in exactly as it did, so that half costs an operator nothing. The rekey itself is derivable over existing data, because each existing account has exactly one user to assign and one identifier to mint. **An address is not derivable for every account**: none is stored today, and configuration states one only where a delivery sender or a user name that is itself an address carries it. The migration writes the address wherever one of those does, carries an account for which neither does with no address, and leaves that account unserved until an administrator sets one, reporting it at startup; the operator's action is to set the address of every account so reported. What decides the classification is the half no migration reaches at all. The release's migration is one idempotent `mailfathom-schema-<version>.sql` an operator applies with `psql -f`, and nothing runs it for them; that file never sees `appsettings.json`, the environment the host binds from, or ADR 0005's key ring, so it can neither import an account only configuration names — and a configured account synchronization never reached has no row to fill — nor seal the credentials this record moves into the database. Applying the script alone would leave a deployment whose accounts and credentials are gone from configuration and absent from the database, which is not deployable over the previous release's data. ADR 0004's tie-break says to take the higher increment, so the release states the major row and says plainly that the operator provisions what they had configured. Two users already declaring one address cannot both keep it under the unique index, so the migration stops naming that address rather than choosing one of them, and the operator resolves it before retrying.

### Organizations group users and scope a login

**A user belongs to no organization or to exactly one.** An organization carries an identifier the deployment generates, a display name, and a short name unique across the deployment, held upper-case.

**An organization does two things and nothing else.** It groups users, so an operator hosting several companies can list and manage each company's people together. And it scopes a Basic username: a member signs in as `SHORTNAME/username` and somebody in no organization as `username`, so two companies can each have a `jan`, and the credential names the organization it is scoped to by identifier rather than by short name, so renaming a short name moves every login with it and rewrites no credential. Methods whose lookups are already unique across the deployment — a minted key, a public key, an authorization server's subject — are not scoped by it.

**What that costs the credential contract is its uniqueness, not its username form.** A login is split at its first `/` into the short name, compared upper-case, and the username, which keeps the form and the canonical fold it has today — so a username still refuses `/`, and nothing an existing credential stores changes. What does change is that a password credential's lookup stops being unique across the deployment: it is unique within its organization, and within the users in none, so the credential row carries the organization and the index that held `(Method, Lookup)` holds the organization between them. A login naming an organization that does not exist fails the way a wrong password does, so a refusal cannot enumerate organizations.

**An organization is not a configuration layer.** No setting is declared on it and inherited by its members, and nothing a member is served changes because they joined one; a setting is the deployment's, the account's, or the person's, as the sections above place it.

**An organization is not an isolation boundary.** Nothing narrows by it: a user reads the accounts assigned to them, whichever organization they and every other assigned user belong to, and an administrator may assign one account to members of two organizations. It separates names, not data. **A second deployment stays the answer to tenancy**, for the reasons *Multi-tenancy is refused rather than deferred* gives — an operator who needs one company's mail unreachable from another's by construction runs two instances, and an organization is deliberately not a cheaper version of that.

### The deployment-wide mechanisms, and which of them stay global

**Job leases and fairness.** `jobs` carries `MailboxAccountId`, and a claim ordered by `AvailableAt` and the identifier alone would be pure FIFO, so one mailbox's backlog would delay every other mailbox's due work for as long as it lasted. The claim is fair across users, ordering by a turn rather than by arrival. This record fixes the property rather than the algorithm: no user's backlog may postpone another user's due work indefinitely, and the claim stays one statement with `FOR UPDATE SKIP LOCKED` in it, because that is what ADR 0009 decided and this changes the order rather than the mechanism.

**Embedding spend ceilings.** A ledger keyed by the period alone lets one user spend the whole window, so the ledger carries a user, and a ceiling is enforced per user *and* per deployment, over the same epoch-anchored windows. A refusal names which of the two it reached.

**The stored-content ceiling.** Issue 889 does not list this one and it is the same fault: the ceiling is `pg_total_relation_size` over `email_message_contents`, so on its own one user's mailbox fills it and every other user's mail is recorded as `AwaitingStorageHeadroom` with no content. A per-user bound stands beside the deployment-wide one, measured per user rather than from the catalog, since the catalog answers for the table.

**The per-user ceilings count a shared account in full against every user it is assigned to.** `MailSynchronization:MaxStoredContentBytesPerUser`, `Embeddings:MaxInputCharactersPerPeriodPerUser`, `Embeddings:AttachmentText:MaxInputOctetsPerPeriodPerUser`, and `Embeddings:ImageDescription:MaxDescriptionsPerPeriodPerUser` each bound what one person's mail may occupy or cost, and a shared mailbox is wholly that person's mail — so its stored content counts toward each assigned user's figure, spend incurred on it counts toward each user assigned when it was incurred, and work on the account proceeds only while every one of those users is under the ceiling. The deployment-wide ceilings count the one copy once, which is what they measure. **The trade-off is stated rather than left implicit:** a user whose own mailboxes have reached their ceiling holds back a shared mailbox for everybody assigned to it, and assigning a large mailbox to a user can put them at their ceiling at once. Dividing the account's figure between its users was declined, because a user's allowance would then move whenever an administrator assigned somebody else to a mailbox they share, and charging it to one of them was declined because no assignment is more the owner than another. The `user_stored_content` accumulator, which exists because the ceiling is consulted before every message, is rekeyed onto the account and renamed `account_stored_content`, since a table keyed by the account and carrying no user would otherwise be named for what it no longer holds — incremented once, in the transaction that stores the payload, for the one copy — and a user's figure is the sum over the accounts assigned to them, so an assignment moves the figure without rewriting a row, and a per-user accumulator incremented by whichever user's run stored the payload is the failure this rule exists to prevent; spend is a record of an event, so it is charged to whoever was assigned at the time and not recharged when that changes.

**The embedding profile stays deployment-wide, and that is a decision rather than an omission.** One registered profile serves everyone, one generation is read at a time, and one backfill fills it. ADR 0006 makes a profile the geometry of a vector space; a profile per user would mean an HNSW index per user, a backfill per user, and an activation spend per user, to let two people search their own mail in different spaces — which nothing asks for, since no query ever spans two users. The operator chooses the model for the deployment, as they choose the database.

**Connections to one mail server become a deployment-wide budget, and they are the mechanism this axis strains hardest.** An IMAP connection authenticates as one mailbox, so nothing about several users lets two mailboxes share one — the reuse a reader reaches for first is not available, and this record says so to stop it being built. What is available is a ceiling per mail server host, held across every account. It is needed because the counts that exist today are keyed to an account and to a run: `MaxConcurrentAccounts` bounds run slots rather than supervisors, and `MailSynchronizationCoordinator` starts one supervisor per served account, so the push session each account holds persists outside that bound, as does the write connection the pool keeps per account for its linger period. Persistent connections therefore scale with the number of accounts — a shared account is one set of connections however many users it is assigned to, which is part of what holding it once buys — while nothing anywhere is keyed by host. `MaxConcurrentFoldersPerAccount` already records the reasoning in the small — one connection per account is the server-friendly choice, and the write connection counts against the server's limit even while nothing is written — and the user axis is what makes the same reasoning necessary in the large. The harm is specific and is not ours alone to absorb: a provider refusing or throttling connections from one address refuses them for **every** user on that provider at once, so one user's account count degrades another user's synchronization through a limit neither of them can see.

**The backfill cursors stay global too.** Each named backfill keeps one position, ordered by the stored-email identifier. That order interleaves accounts by construction, so the walk is already fair, and a cursor per user would multiply the cheapest part of the system. This is written down so it is not later mistaken for something the user axis forgot.

### Vector search, and what partitioning is deferred with

The ranking is exact and does not use the approximate index that exists. That is `EmailVectorSearchIndexReader`'s own decision, argued in its remarks: the structural filters join the ranking rather than trailing it, so post-filtering cannot measure the query against mail the caller may not see, and an HNSW scan orders the whole table and cannot carry a filter on a joined table.

**Ownership does not change this and must not be blamed for it.** The narrowing to the caller's assigned accounts is the account predicate the query already carries. Whether the ranking should become approximate is a retrieval question about corpus size, it is open today at one user, and it is opened separately rather than folded in here.

**Partitioning is deferred, and the cost of deferring is named.** It is not decided now because the predicate that would benefit from it is already an account predicate, and no measurement exists at a corpus size that would justify the change. What deferral costs is that partitioning `stored_emails` or `email_embeddings` afterwards, under an append-only migration rule, is a table rewrite rather than a migration — so this is a decision to revisit against a measurement, not a step that can be taken absent-mindedly later.

### Migrations, pooling, and schema versions

Refusing multi-tenancy answers all three, and it is worth stating what it preserves rather than only what it avoids. There is one database, so there is one schema, one `__EFMigrationsHistory`, one snapshot the pull-request gate compares against, and one artifact that brings a database to a release — the four things ADR 0009 names as a contract. The application never has to tolerate two schema versions at once. One connection pool serves one database, so no transaction-mode pooler is required, and Npgsql's automatic statement preparation and the safety of transaction-scoped session settings both remain assumptions this deployment may make.

### Contacts belong to the user who wrote them or the account they arrived on

**A user's own contacts belong to the user.** A person somebody wrote down is part of that person's book, keyed onto the user record, and nobody else assigned to any of their accounts reads or amends it.

**Collected contacts belong to the account they arrived on.** Collection records an address because mail reached a mailbox, so the record is a fact about the mailbox rather than about whoever happened to be reading it, and contact collection is a setting of the account like every other. Every user assigned the account reads what it collected, and a collected contact keys onto the account rather than onto a user, so it cascades from the account row and goes with the account; a contact a user wrote down keys onto the user and cascades from the user row, and neither kind is reached through the other.

**A collected contact stored before this amendment has no account to key onto**, since collection recorded it against a user. Where that user holds exactly one account, the migration keys it onto that account, which is the only mailbox it can have arrived on; where the user holds several, nothing says which, so it stays in that user's book as the collected contact it was — shown to them as before and to nobody else — rather than being guessed onto a mailbox other users read or discarded.

**A read merges the two, and the user's own record wins.** A user is shown their own contacts and the collected contacts of every account assigned to them as one book; where both hold one address, the record the user wrote is the one shown, because a person's own words about somebody outrank what arriving mail implied.

### Erasure

A shared database, so erasure is a cascade rather than a `DROP DATABASE` — but the cascade reaches less than the model's use of `MailboxAccountId` suggests, and that gap is the substance of this section rather than a caveat on it.

**Erasing a user removes that user's assignments, and removes an account with its mail when that was its last assignment.** The user's own record goes with them — their credentials, their own contacts, their preferences, their sessions, and the drafts they authored and the recurring sends they declared in any account — and so does every assignment naming them. An account still assigned to somebody else stays whole: its mail, its settings, its collected contacts, and its audit trails are the mailbox's rather than the erased user's, and every remaining user goes on reading them. An account the erasure left with no assignment is erased with everything beneath it, because mail no user is served is mail the deployment holds for nobody. **Unassigning an account's last user by any other route does the same**, so an administrator moving a mailbox from one person to another assigns the new user before unassigning the old one.

**Four foreign keys point at `mailbox_accounts`**: `MailFolderEntity`, `EmailThreadEntity`, and `JobEntity`, and the collected contacts this record keys onto the account. So deleting an account takes its folders, and the folders take their stored mail, and the mail takes its content, its search documents, its chunks, its chunks' embeddings, its classifications and their signals, its repair requests, the audited-email rows recording that an answering run cited it, the rule executions recording what was decided about it, and the mailbox mutations, which cascade from their folder as well as from the mail; the threads, the jobs, and the collected contacts with their addresses go with the account directly, and the thread identifiers go with the threads, reached through `email_thread_identifiers`' key onto `email_threads` rather than through one onto the account. That much is the foreign keys' own work and needs nothing remembered.

**Every other table naming an account carries `MailboxAccountId` as a plain bounded value with no key onto one**, and none of those is erased by deleting the account: the mutation audit entries, the rule evaluation runs, the answering audit entries, the spam classification runs, the rederivation positions and runs, the drafts, the recurring sends, the outgoing mail, the sealed `stored_secrets` an account declares, its `account_stored_content` figure and its `stored_content_claims`, and the sealed `MailboxRefreshTokenEntity` — whose own mapping says it, that removing an account has to remove the row deliberately rather than by cascade, and that this is the erasure seam's job rather than the schema's.

**The rows hanging off those survive with them rather than on their own account**, and the distinction matters to whoever writes the seam. An outgoing email's recipients and its content name no account at all, and its filings name one without a key onto it; all three cascade from the outgoing email, so deleting that message takes them and nothing has to enumerate them. What the seam has to reach is the outgoing email itself, since no key deletes it with the account. The seam therefore lists the tables that name an account and no key reaches, and lets each one's own cascades carry what hangs beneath it — which is also why `email_thread_identifiers` and the audited-email rows belong to the paragraph above rather than to this list, both being reached by a cascade despite naming an account or hanging off one that does.

So the honest statement is the narrower one: **ownership on the account makes erasing a mailbox one statement plus a named list, rather than one statement alone.** What it buys over the discriminator this record rejected is that whose mail a row is gets *derived from the account's assignments* rather than decided per message, so the list of tables to reach is finite and enumerable from the model instead of every repository being asked whether it kept a copy of a per-message classification. The seam that discharges it exists — `UserAccountErasure` derives the tables no cascade reaches from the model and deletes their rows by the user — and this amendment takes its key away: most of those tables stop carrying a user, so the seam deletes by the accounts the erasure left with no assignment, and keeps deleting by the user only for the drafts and recurring sends that name their author. Whether it goes on discharging that list or the missing foreign keys are added is work this record authorizes rather than something it assumes; a ledger row recording spend a user incurred — retained as a cost record rather than erased with the vectors it paid for — stands outside it for a reason of its own. An entry in a shared account's audit trail naming the erased user as the one who acted is part of the mailbox's record rather than of the user's, and what the seam does with that reference is the erasure seam's to state rather than something this record leaves to be discovered.

### The administrative surface gains a deployment administrator

An administrative entry that writes no grant reaches every administrative operation. That is the default rather than the whole posture: ADR 0012 shipped, the administrative group carries a filter that refuses a route mapped without stating a permission (`AdminApiEndpoints`, on the group rather than on each route, so forgetting to decide fails closed), and an entry that narrows its grant is already bounded — so the published permissions distinguish operations, and what they do not distinguish is principals. Several users require an administrator distinct from a user — issue 889 calls it a tenant administrator, and since this record refuses tenants it is a **deployment administrator**.

This is what the record adds beside ADR 0012 rather than a change to what it decided. Its decision is not reopened: it publishes a closed set of named capabilities, that set is unchanged in vocabulary and in meaning, and a deployment administrator is a principal holding administrative ones while an ordinary user holds none of them. What is amended there is its **context**, in the five sentences that assumed this decision had not been taken — the decision driver stating that the deployment serves one user's mail, the two passages explaining why the account bound was excluded, the note on issue 588, and the revisit trigger, which named a deployment serving more than one user's mail as exactly the event that reopens the accounts axis. That trigger has fired, and a record whose own condition has been met has to say so or send its reader nowhere. ADR 0012 is still `proposed` and therefore editable, which is what that status is for. A user's own maintenance operations — reindexing their mail, reading their own histories, disposing of their own folders — are scoped to their assigned accounts by the same resolution every other read uses. **What destroys mail other people read is not a user's to do**: on an account assigned to anybody else, disposing of a folder or of the account's mail is refused to a user and is an administrator's, and a user removing a shared account from their own list unassigns only themselves. On an account assigned to them alone, they may do all three, and removing it is unassigning its last user, which erases it as *Erasure* states. Creating an account for a user, assigning it to another, and managing organizations are the deployment administrator's.

### The order the work is delivered in

Steps 1 through 6 delivered the single-owner shape this amendment builds on; where step 7 or later replaces what one of them put in place, the later step governs, and steps 1 and 4 are read with that in mind.

1. **The user on the account**, with the user it names. This is the irreversible one and it goes first; step 7 replaces the user on the account with assignments.
2. **The principal carries the user, and the account catalog splits** into a caller-scoped and a process-wide answer. The account listing moves onto the caller-scoped one in the same step, because it is what publishes the account set and it resolves that set from the catalog directly.
3. **The four identifier reads and the send** ask about the caller's user as well as about the account's settings. The send belongs with them rather than later, because it is the one path where getting ownership wrong sends mail as somebody else rather than showing it to the wrong reader.
4. **The contact book gains a user**, which is the one schema change that is genuinely awkward and is described on its own issue; step 10 keys collected contacts onto the account instead and leaves a user's own contacts on the user.
5. **The ceilings and the job claim** gain the user axis.
6. **Accounts, users, and credentials leave configuration** into the database and an administrative surface.
7. **A mail account becomes a record of its own**, with a generated identifier, a deployment-unique address, every setting of the account, and assignments to users.
8. **The mail graph is rekeyed onto the generated identifier**, every narrowing site reads the caller's assigned accounts, every composed identity names the generated identifier, and the per-user ceilings count a shared account as stated above.
9. **The attachment ticket checks the assignment**, which is the only bound on the one path with no caller, so it belongs with the narrowing rather than after it.
10. **Collected contacts move onto the account they arrived on**, and erasure discharges a shared account as stated above.
11. **Organizations group users and scope a Basic username**, which needs the credentials step 6 moved into the database and runs beside steps 7 through 10.

Steps 2, 3, 8, and 9 are what make isolation true under shared accounts — step 1's user on the account is what step 8 replaces. The rest are what make a second user, a shared mailbox, and a second company usable. Nothing here is delivered by a release this record names.

### Consequences

- Good, because ownership hangs on the account, which is the one row per mailbox the whole mail graph already cascades from, rather than on a classification of each stored message.
- Good, because a mailbox several people read is held once: one copy of its mail, one set of connections to its server, one synchronization, and one set of settings that cannot disagree with the server about where a message is.
- Bad, because a setting one assigned user changes reaches every other, which is the correct answer for one mailbox and still a surprise to somebody who thought of it as theirs.
- Good, because the generated identifier is unique across the deployment, so every row, index, job, lease, and composed identity carrying it resolves to one account, and an administrator naming an account can never be answered with somebody else's. On the four tables keying onto the account — the folders, the threads, the jobs, and the collected contacts — a reference is a reference to exactly one; elsewhere the account stays a plain column, so this buys an unambiguous value to narrow on rather than an enforced narrowing, which stays the shared predicate's and the tests' job.
- Bad, because the mail graph is rekeyed onto the generated identifier with the index rebuild that follows, and because the MCP tool contract publishes a generated UUID where an operator used to read a name they typed.
- Neutral, because a mail row stops carrying a user, so the unconditional user term that backs up the resolver's answer for a user assigned no account today goes away; that answer being a scope that admits nothing is the one place the mechanism fails open if it is got wrong, and a test holds it.
- Neutral, because the timeline across several accounts stays a merged per-account walk in `StoredEmailTimelineReader.MergedPage`, there being no leading column that serves it in one walk.
- Bad, because the per-user ceilings count a shared mailbox in full against every assigned user, so one user's own mailboxes can hold back a mailbox their colleagues share.
- Good, because one schema, one migration chain, one snapshot gate, and one upgrade artifact all survive, and ADR 0009's contract is preserved rather than renegotiated.
- Good, because erasing a user is removing their record, their assignments, and the drafts and recurring sends they authored, and erasing a mailbox that nobody is left assigned is one delete against `mailbox_accounts` plus a list enumerable from the model, rather than a question asked of every repository — though only four foreign keys point at the account, so that list is real work and the erasure section names it.
- Good, because an operator who genuinely needs tenant separation gets a stronger answer than a database per tenant would have given them, with no routing to get wrong, and an organization gives several companies their own logins without being mistaken for that separation.
- Neutral, because isolation rests on query predicates, on one method asked in three places, and on the attachment ticket's assignment check, with no cryptographic separation between users; the data-encryption key ring under ADR 0005 stays deployment-wide and covers refresh tokens rather than mail.
- Neutral, because the embedding profile, the backfill cursors, and the model choice stay deployment-wide, so users share retrieval geometry and an operator's model decision reaches everyone.
- Bad, because a contact book read merges two sources — the user's own contacts and the collected contacts of every account assigned to them — so what used to be one table read is a merge with a precedence rule.
- Bad, because accounts, users, and credentials leaving configuration is a larger change than the ownership axis itself and reaches the surface ADR 0002 draws, even though it costs that record nothing.
- Bad, because the change reaches three of ADR 0004's four public surfaces and all three break: the MCP tool contract, the configuration schema, and the database schema, which takes the major row because the release's schema script can rekey accounts already stored but cannot import one only configuration names — so an operator has to provision what they had configured, re-point any client naming an account by a typed identifier, and resolve any address two users both declared before the migration completes.
- Bad, because a deployment serving several users reaches the synchronization coordinator's `MaxConcurrentAccounts` sooner than one serving one, and that bound is read once at startup.

## Validation

- A test asserts that every caller-facing use case resolving an account composes the caller-scoped account catalog — the reads and the send alike, since the send returns no mail and an assertion about reads would pass while it resolved across users — and that the operations answering for the deployment compose the process-wide one. Both are resolvable from `Application` because use cases there need each, so injecting the wrong one compiles — which is exactly why this is a test rather than a property of the composition.
- A unit test asserts that all three selection-driven mailbox read models — the timeline, the lexical ranking, and the vector ranking — compose `StoredEmailSelectionPredicate.Matching`, which is what makes the narrowing a property of the system rather than of each query. The thread read, which is read by membership and so composes `StoredEmailSelectionPredicate.WithinScope` rather than `Matching`, is held to the same narrowing by the same test, because it is the fourth path that returns mail.
- A unit test asserts that a caller whose user is assigned no account reads nothing from every one of those four paths, rather than reading everything a non-empty account list would have restricted.
- A unit test per identifier read asserts that an email belonging to an account not assigned to the caller's user is answered as absent, in the same shape as one that does not exist.
- A test shows two users assigned one account reading one message, a third user reading nothing, and a user assigned only one account refused a submission from another.
- A test asserts that every entity referencing a mail account references it by the generated identifier, which is what makes one copy per account a property of the model rather than of each mapping somebody remembered to write.
- A test asserts that no job schedule identity, and no idempotency key derived from one, is composed from anything but the generated account identifier. This is the half the model cannot hold at all: the schema reaches no string, so a schedule source added later and keyed on a display name would reintroduce the failure this record describes with nothing refusing and nothing erroring.
- A unit test asserts that an address already held anywhere in the deployment is refused to a user adding it through the client in the same shape as a refusal for any other reason, and that two writers claiming one address are separated by the unique index.
- A unit test asserts that erasing a user leaves an account still assigned to somebody else whole, and erases an account whose last assignment it removed.
- A unit test asserts that an attachment ticket naming an email in an account no longer assigned to the user the ticket was minted for is refused in the same shape as an unknown ticket, which is the one identifier read with no caller to ask about.
- A unit test asserts that a draft and a recurring send are each read only by their author among an account's assigned users, that erasing the author of a recurring send on a still-assigned account stops it, and that a password credential named `jan` may exist once in no organization and once in each organization while a second one in either is refused.
- `Fathom review` reads this record's `describes:` marker, so a change under the paths it names is told which decision it is being read against.
- A test asserts that no mail-reading path composes raw SQL, which is the condition under which this record's refusal of row-level security stops holding.
- The breaks named against the MCP tool contract, the configuration schema, and the database schema are carried into `CHANGELOG.md` by the release pull request, per ADR 0004, each with the operator's action.

## Pros and Cons of the Options

### One tenant, several users, ownership on the mail account

A user is assigned accounts, an account is a record of its own holding one copy of its mail, and the resolution that already narrows a mailbox read narrows it to the caller's assigned accounts.

- Good, because the data model reaches every mail table from the account already, so ownership hangs on one row per mailbox rather than being decided per message.
- Good, because one mailbox read by several people is one account assigned to each of them, with one copy of its mail.
- Neutral, because keying the mail graph by a generated account identifier is a rekey of every table that names an account, which is the cost this record takes in exchange for a key that cannot mean two accounts at once.
- Good, because the scoped reads share one predicate and the identifier reads share one method, so the number of places ownership has to be got right is small and each is named: the shared narrowing, which the thread read composes too, the one method the four identifier reads ask, the ticket the attachment download redeems, the account the send resolves, and the set the account listing publishes.
- Good, because it leaves one database, which keeps the schema contract, the pooling assumptions, and the upgrade artifact exactly as they are.
- Good, because narrowing one credential within its user's accounts is refused rather than left open, so what a caller reaches is one scoping rule rather than two that would have to agree; issue 588 is closed as not planned.
- Bad, because isolation is enforced by the application rather than by the database, so a mail-reading path written outside the shared predicate would bypass it.

### Several tenants, a PostgreSQL database per tenant, with a role per tenant

Each tenant's data in its own database, reached by a role of its own, with routing above.

- Good, because it is the strongest separation short of separate processes, and it concentrates the attack surface into one auditable place — the resolution of which database.
- Good, because erasure is a `DROP DATABASE` and discharges a deletion request completely.
- Good, because query performance improves: smaller indexes and no filtered approximate search.
- Neutral, because it needs the user axis underneath it anyway once a tenant holds more than one user, so it is additional work rather than alternative work.
- Bad, because migrations multiply by the tenant count against an explicitly non-automatic migration step, so either the application runs against schema *N* and *N-1* at once or every release needs a window per tenant.
- Bad, because connection pooling multiplies too, and a modest tenant count times a minimum pool exceeds a default `max_connections`; the way out is a transaction-mode pooler, which withdraws Npgsql's automatic preparation and invalidates transaction-scoped session settings as a safe assumption.
- Bad, because ADR 0009's job store assumes one database, so the scheduler would acquire one store per tenant to rotate over.
- Bad, because everything it buys is already bought by running the deployment twice, which costs a container and a database.

### Several tenants inside one database, separated by a tenant discriminator and row-level security

One database, a tenant column on every table, and PostgreSQL policies below the ORM.

- Good, because it needs no routing and keeps one schema and one pool.
- Good, because the policy holds below any query the application writes, including one written wrong.
- Neutral, because it needs the same user axis underneath, so the user layer is unchanged by it.
- Bad, because a per-request identity carried in a session setting without `SET LOCAL` survives into the next request on the same pooled Npgsql connection, so the mechanism has to be designed around a trap.
- Bad, because the policy is decoration while the application role holds `BYPASSRLS`, which no statement overrides. Ownership is not part of that cost: the serving role owns `email_embeddings` to create and drop the approximate index at profile activation, and `ALTER TABLE ... FORCE ROW LEVEL SECURITY` binds a policy to an owning role.
- Bad, because it buys a second line of defence against a class of defect — raw SQL reading mail — that does not exist in this repository.

### Stay single-user and refuse the whole question

Leave accounts in configuration and the deployment serving one user's mail.

- Good, because it is free and nothing is at risk.
- Neutral, because it is what every release so far has shipped and nothing is broken by it.
- Bad, because it leaves the question waiting on a decision indefinitely — issue 588 stood open on it for exactly that reason — and every issue presupposing more than one principal fixes the answer in its own implementation diff.
- Bad, because the ownership axis only gets more expensive as the corpus grows, and refusing to decide is itself a decision to pay more later.

## More Information

- Issue 889 asks the question this record answers. Issue 588 waited on it and is closed as not planned: the one axis this record was once read as leaving to it is refused above, and issue 1313 is where that refusal was written into this record and into ADR 0012.
- Issue 1949 is where mail accounts shared between users and organizations were decided, on 2026-09-13, as an amendment to this record rather than a superseding one because this record is `proposed` and an ADR at that status is editable — the same ground on which this record amends ADR 0012's context. Its parent, issue 1955, carries the work: issue 1950 the account record and its assignments, issue 1325 the rekey of the mail graph and every narrowing site, and issue 1951 organizations; issues 1329, 1348, and 1346 follow for collected contacts, the attachment ticket, and erasure.
- ADR 0002 closes the configuration write side and sends state a program modifies to PostgreSQL, which is the authority under which accounts, users, and credentials move.
- ADR 0004 permits every break named here and requires each to be recorded against its surface.
- ADR 0005 keeps the data-encryption key ring deployment-wide; nothing here divides it per user.
- ADR 0010 keeps a rule set in configuration and is not amended: this record decides only that a rule acts on the account it names, and that it names one by address or generated identifier.
- ADR 0006 makes an embedding profile the geometry of a vector space, which is why the profile stays deployment-wide.
- ADR 0009 states the one-schema contract this record preserves and owns the job claim whose ordering it changes.
- ADR 0012 publishes the closed permission set this record adds an ownership axis beside, without reopening it, and gains a deployment administrator distinct from a user. Its context is amended in this change — the single-user driver, the account-bound exclusion, the issue 588 note, and the revisit trigger this decision fires — while its decision stands.
- Revisit this decision if an operator case appears that a second instance genuinely cannot serve — shared infrastructure a tenant may not have its own of, or a tenant count high enough that per-instance overhead dominates. Revisit the partitioning deferral against a measurement of a corpus large enough for the account predicate to stop being selective enough on its own.
