<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- Where Aspire persists the values it generates for the app model itself — the dashboard and OTLP API keys —
         rather than anything this repository owns. The PostgreSQL credentials are not among them: they are stated in
         the app model, which is what keeps a password and a data volume that outlives it from ever diverging. The value
         is a fixed identifier, and what Aspire writes under it lives in the developer's own profile. -->
    <UserSecretsId>aspire-apphost-6f1c0a24-8a2c-4f5c-9d3a-4c2d0f8b71e5</UserSecretsId>
  </PropertyGroup>
  <Sdk Name="Aspire.AppHost.Sdk" Version="13.4.6" />

  <ItemGroup>
    <PackageReference Include="Aspire.Hosting.AppHost" />
    <PackageReference Include="Aspire.Hosting.EntityFrameworkCore" />
    <PackageReference Include="Aspire.Hosting.PostgreSQL" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Host\Host.csproj" />
    <!-- Referenced so the project that owns the DbContext and the migrations is built before a migration command runs
         against it, and marked as not a resource because it is a library: the orchestration launches Host, and the
         migration resource names this project by path rather than as something to start. -->
    <ProjectReference Include="..\Infrastructure\Infrastructure.csproj" IsAspireProjectResource="false" />
  </ItemGroup>
</Project>
